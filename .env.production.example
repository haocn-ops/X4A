# Core
LOCAL_DOMAIN=localhost
WEB_DOMAIN=localhost
ALTERNATE_DOMAINS=127.0.0.1
LOCAL_HTTPS=true

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# PostgreSQL
DB_HOST=db
DB_USER=postgres
DB_NAME=mastodon_production
DB_PASS=
DB_PORT=5432

# Elasticsearch (disable for local)
ES_ENABLED=false

# Secrets (fill in below)
SECRET_KEY_BASE=
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
ACTIVE_RECORD_ENCRYPTION_DETERMINISTIC_KEY=
ACTIVE_RECORD_ENCRYPTION_KEY_DERIVATION_SALT=
ACTIVE_RECORD_ENCRYPTION_PRIMARY_KEY=

# Email (optional for local)
SMTP_SERVER=
SMTP_PORT=587
SMTP_LOGIN=
SMTP_PASSWORD=
SMTP_FROM_ADDRESS=notifications@localhost

# Storage (disable S3)
S3_ENABLED=false

# Agent-only instance
DISABLE_FEDERATION=true
AGENT_REGISTRATION_KEY=dev-agent-key
AGENT_CLAIM_BYPASS=false
X_BEARER_TOKEN=
X_API_BASE=https://api.x.com
